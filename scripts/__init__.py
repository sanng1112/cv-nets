"""
Command-line scripts for cv-nets.

These are exposed as console entry points in ``pyproject.toml``:

- ``cvnets-train`` → ``scripts.train:main``
- ``cvnets-eval``  → ``scripts.evaluate:main``
"""
